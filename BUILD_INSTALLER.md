# Создание установщика и EXE файла

## Требования перед сборкой:

✅ Все зависимости установлены (`npm install` выполнен)
✅ Иконка приложения находится в `assets/amethyst_launcher.png` (256x256 или больше)

## Команды сборки:

### 1. Создать портативный EXE файл (не требует установки)
```bash
npm run dist:dir
```
Создаст: `Amethyst-Launcher-3.0.0.exe` (портативный файл)

### 2. Создать установщик NSIS + портативный EXE
```bash
npm run dist:win
```
Создаст:
- `Amethyst Launcher Setup 3.0.0.exe` (установщик NSIS)
- `Amethyst-Launcher-3.0.0.exe` (портативный)

### 3. Создать все установщики (полная сборка)
```bash
npm run dist
```

## Где найти созданные файлы:

После выполнения команды сборки файлы будут в папке:
```
dist_electron/
├── Amethyst Launcher Setup 3.0.0.exe       (установщик)
├── Amethyst-Launcher-3.0.0.exe            (портативный)
└── ...другие файлы
```

## Процесс сборки:

1. **Сборка React приложения** (Vite)
   - Компилирует TypeScript/JSX в JavaScript
   - Минификация CSS и JS
   - Оптимизация бандлов

2. **Сборка Electron приложения** (electron-builder)
   - Упаковка всех файлов
   - Подготовка для installer
   - Генерация EXE файлов

Процесс может занять **2-5 минут**.

## Что происходит в установщике:

**NSIS Installer (Setup.exe):**
- Показывает диалог выбора директории установки
- Создает ярлыки на рабочем столе и в меню "Пуск"
- Добавляет приложение в "Программы и компоненты"
- Позволяет удалить через "Программы и компоненты"

**Портативный EXE:**
- Просто запускает приложение
- Не требует установки
- Можно запустить с флешки или любой папки

## Распространение:

Вы можете распространять:

1. **Setup файл** (`Amethyst Launcher Setup 3.0.0.exe`)
   - Рекомендуется для конечных пользователей
   - Устанавливается как обычное приложение Windows

2. **Портативный EXE** (`Amethyst-Launcher-3.0.0.exe`)
   - Для продвинутых пользователей
   - Для ноутбуков и портативных устройств

3. **Через GitHub Releases**
   - Загрузить оба файла в релиз
   - Система автообновления будет скачивать их

## Обновление версии:

Для создания новой версии:

1. Обновить версию в `package.json`:
   ```json
   "version": "3.1.0"
   ```

2. Обновить в `main.js`:
   ```javascript
   const APP_VERSION = '3.1.0';
   ```

3. Создать новый релиз на GitHub с тегом `v3.1.0`

4. Пересобрать:
   ```bash
   npm run dist:win
   ```

5. Загрузить файлы в GitHub Release

6. Система автообновления подхватит новую версию!

## Отладка при сборке:

Если возникают ошибки, проверьте:

- Все файлы из `files` в `package.json` существуют
- Иконка в правильном формате
- Нет синтаксических ошибок в коде
- `node_modules` не повреждена (`npm install`)

## Команда полной очистки перед пересборкой:

```bash
rm -Recurse -Force dist_electron
npm run dist:win
```

---

**Готово к сборке!** 🚀
