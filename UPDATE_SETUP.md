# Auto-Update Configuration Guide

## Что было добавлено:

✅ **Автоматическая проверка обновлений** на GitHub при запуске приложения
✅ **Автоматическое скачивание** новой версии если доступна
✅ **Отображение прогресса** скачивания на splash screen
✅ **Сравнение версий** для определения необходимости обновления

## Как это работает:

1. При запуске лаунчера проверяется последний релиз на GitHub
2. Сравнивается текущая версия (из `package.json`) с последней
3. Если доступна новая версия - она скачивается в фоне
4. Прогресс отображается на splash screen
5. После скачивания приложение перезагружается

## Требуемые действия:

### 1. Обновить GitHub репозиторий данные в `main.js`:

Найдите эти строки в начале файла (строки 13-16):
```javascript
const GITHUB_REPO = 'Nominor/Amethyst';     // Измените на ваш репо
const GITHUB_OWNER = 'Nominor';             // Измените на ваше имя
const GITHUB_REPO_NAME = 'Amethyst';        // Измените на имя репо
```

Замените на ваши данные:
```javascript
const GITHUB_REPO = 'ВашИмя/ИмяРепо';      // например: 'Nominor/Amethyst'
const GITHUB_OWNER = 'ВашИмя';              // например: 'Nominor'
const GITHUB_REPO_NAME = 'ИмяРепо';        // например: 'Amethyst'
```

### 2. Создать релизы на GitHub:

Каждый релиз должен:
- Иметь тег вида `v3.0.0`, `v3.0.1`, и т.д.
- Содержать бинарные файлы (`.exe` или `.zip`)
- Включать исполняемое приложение

**Пример структуры релиза:**
```
Release: v3.1.0
- Tag: v3.1.0
- Asset 1: Amethyst-Setup-3.1.0.exe (или другое имя с версией)
- Asset 2: Amethyst-3.1.0.zip
```

### 3. Обновить версию в `package.json`:

При каждом новом релизе обновляйте версию:
```json
{
  "version": "3.0.0"  // Измените эту версию
}
```

## Отключение проверки обновлений:

Если нужно отключить автообновление (например, для тестирования), закомментируйте эту строку в `main.js`:

```javascript
// В функции createWindow:
checkAndDownloadUpdate(mainWindow).then(updateInfo => {
  // ... код
});
```

Замените на:
```javascript
// Отключено для тестирования
// checkAndDownloadUpdate(mainWindow).then(updateInfo => {
//   // ... код
// });

// Просто загружаем главное окно
setTimeout(() => {
  const indexPath = path.join(__dirname, 'dist', 'index.html');
  mainWindow.loadFile(indexPath);
}, 14500);
```

## Логирование:

Все операции обновления логируются в консоль:
- ✅ Текущая версия
- 📦 Последняя версия на GitHub
- 🆕 Доступно ли обновление
- 📥 Имя файла для скачивания
- 📊 Размер файла
- 💾 Путь сохранения
- 📊 Прогресс скачивания (%)

## Примеры сообщений:

```
🔄 Checking for updates...
📦 Current version: 3.0.0
📦 Latest version: 3.1.0
🆕 New version available: 3.1.0
📥 Downloading: Amethyst-Setup-3.1.0.exe
📊 Size: 45.32 MB
💾 Saving to: C:\Users\...\AppData\Roaming\Amethyst\updates\Amethyst-Setup-3.1.0.exe
📊 Download progress: 45%
✅ Download complete!
```

## Безопасность:

- Используется HTTPS для скачивания
- Проверяется наличие файлов перед использованием
- При ошибке скачивания - приложение продолжает работу нормально
- Скачанные файлы хранятся в AppData в папке обновлений

---

**Готово!** Автообновление лаунчера теперь работает! 🚀
